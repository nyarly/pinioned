<%= form_for(@folk) do |f| %>
  <% if @folk.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@folk.errors.count, "error") %> prohibited this folk from being saved:</h2>

      <ul>
      <% @folk.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
